// Import hodgepodge.
use hodgepodge::*;

fn main() {
    // Print hex codes
    println!(
        "#{:06x}, #{:06x}, and #{:06x} are the RGB colors",
        RGB::Blue,
        RGB::Red,
        RGB::Green
    );
}
